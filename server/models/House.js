class House {
    constructor(data) {
        //do stuff
    }

    static findHouseById(id) {
        let house;
        return house;
    }

    static findHousesByPostcode(req) {
        const minAge = req.query.minAge
        const maxAge = req.query.maxAge
        const size = req.query.size
        let houses;
        return houses;
    }

}